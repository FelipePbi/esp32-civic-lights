#pragma once

/* Pull the production snapshot contract while shadowing only ESP-IDF/NimBLE
 * dependencies that are unavailable in the Windows host test. */
#include "../../../main/sp624e/sp624e_controller.h"
